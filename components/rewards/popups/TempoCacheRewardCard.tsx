"use client";
import { RewardIcon } from "../RewardIcon";
import type { RewardPresentationModel } from "@/lib/blundr/rewards/rewardPresentationAdapter";
export function TempoCacheRewardCard({ reward, revealed, reducedMotion }: { reward: RewardPresentationModel; revealed: boolean; reducedMotion: boolean }) {
  return <div className={`tempo-deck ${revealed ? "is-revealed" : ""} ${reducedMotion ? "is-reduced" : ""}`}><div className="tempo-deck__rear tempo-deck__rear--right" aria-hidden="true" /><div className="tempo-deck__rear tempo-deck__rear--left" aria-hidden="true" /><div className="tempo-deck__flip" data-revealed={revealed ? "true" : "false"}><div className="tempo-deck__face tempo-deck__front" aria-hidden={revealed}><div className="tempo-deck__mark">♔</div><strong>Blundr</strong><span>Tempo Cache</span><small>Reward Deck</small></div><div className={`tempo-deck__face tempo-deck__reward tempo-deck__reward--${reward.rarity}`} aria-hidden={!revealed} data-reward-type={reward.rawRewardType}><RewardIcon reward={{ rewardType: reward.rawRewardType, rarity: reward.rarity, displayName: reward.displayName }} variant="rewardIcon" /><strong>{reward.rewardType === "repertoire_points" || reward.rewardType === "epic_bonus" ? "+" : ""}{reward.amount}</strong><span>{reward.displayName}</span><small>{reward.description}</small><em>{reward.rarity}</em></div></div></div>;
}
