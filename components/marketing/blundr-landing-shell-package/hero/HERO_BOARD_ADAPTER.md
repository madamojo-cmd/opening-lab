# Hero board adapter

The hero canvas is a deterministic marketing visual. If the production board is preferred, replace only the `<canvas>` stage with the existing board component. Keep white orientation, the perspective board sizing, Tempo pointer-follow treatment, `f4` target chip, and `repertoire → train → review` label. Do not connect the hero to trainer, account, or game state. The removed numbered walkthrough links are intentionally absent.
