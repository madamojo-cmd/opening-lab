import { FOOTER_GROUPS } from "../shared/marketing-content"; import styles from "./BlundrMarketingFooter.module.css";
export function BlundrMarketingFooter(){return <footer className={styles.footer}><div className={styles.grid}><div><strong>Blundr</strong><p>Opening training built for the positions you actually face.</p></div>{Object.entries(FOOTER_GROUPS).map(([group,items])=><nav key={group} aria-label={group}><strong>{group}</strong>{items.map(([label,href])=><a key={label} href={href}>{label}</a>)}</nav>)}</div><small>© {new Date().getFullYear()} Blundr. All rights reserved.</small></footer>}
