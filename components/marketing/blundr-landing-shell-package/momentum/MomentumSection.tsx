"use client";
import { useEffect, useState } from "react";
import { MarketingButton } from "../shared/MarketingButton";
import { momentumDemo } from "./momentum-demo";
import styles from "./MomentumSection.module.css";
export function MomentumSection({ tempoImageSrc }: { tempoImageSrc: string }) { const [points,setPoints]=useState(0),[collected,setCollected]=useState(false); useEffect(()=>{const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;if(reduced){setPoints(10);setCollected(true);return}let raf=0,start=performance.now();const tick=(now:number)=>{const e=(now-start)%momentumDemo.durationMs;setPoints(e<momentumDemo.countUpMs?Math.round(e/momentumDemo.countUpMs*10):10);setCollected(e>=4700);raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick);return()=>cancelAnimationFrame(raf)},[]);return <section id="momentum" className={styles.section} aria-labelledby="momentum-title"><div className={styles.copy}><p className={styles.eyebrow}>Momentum</p><h2 id="momentum-title">Turn progress into momentum.</h2><p className={styles.emphasis}>Training should feel as rewarding as improvement.</p><p>Complete your daily training, earn rewards, and watch the work you put in accumulate across your repertoire.</p><p className={styles.close}>Small wins give you another reason to come back tomorrow.</p></div><div className={styles.card}><span>COMMON</span><img src={tempoImageSrc} alt=""/><h3>Reward earned</h3><strong>{points} pts</strong><button type="button" disabled={points<10} onClick={()=>setCollected(true)}>{collected?"Collected ✓":"Collect"}</button></div></section> }
