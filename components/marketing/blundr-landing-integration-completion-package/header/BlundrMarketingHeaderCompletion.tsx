"use client";
import { useEffect, useState } from "react"; import { MarketingButton } from "../shared/MarketingButton"; import styles from "./BlundrMarketingHeaderCompletion.module.css";
const links=[["Why Blundr","why-blundr"],["Daily","daily-blundr"],["Review","smart-review"],["Repertoire","repertoire-mastery"],["Plans","pricing"]] as const;
export function BlundrMarketingHeaderCompletion({ tempoImageSrc }: { tempoImageSrc:string }){const [open,setOpen]=useState(false);useEffect(()=>{const key=(e:KeyboardEvent)=>{if(e.key==="Escape")setOpen(false)};addEventListener("keydown",key);return()=>removeEventListener("keydown",key)},[]);const nav=links.map(([label,id])=><a key={id} href={`#${id}`} onClick={()=>setOpen(false)}>{label}</a>);return <header className={styles.header}><div className={styles.inner}><a className={styles.brand} href="#why-blundr"><span><img src={tempoImageSrc} alt=""/></span>Blundr</a><nav className={styles.desktop}>{nav}</nav><div className={styles.actions}><a href="/login">Log in</a><MarketingButton href="/signup?next=/onboarding/welcome">Start training free</MarketingButton></div><button type="button" aria-expanded={open} aria-controls="bm-mobile-nav" onClick={()=>setOpen(v=>!v)}>{open?"Close":"Menu"}</button></div>{open&&<nav id="bm-mobile-nav" className={styles.mobile}>{nav}<a href="/login">Log in</a><MarketingButton href="/signup?next=/onboarding/welcome" className={styles.mobileCta}>Start training free</MarketingButton></nav>}</header>}
