import type { Metadata } from "next"; import { ContentPage } from "../../components/ContentPage"; import { pageMetadata } from "../../lib/site"; import { games } from "../../lib/content";
export const metadata: Metadata = pageMetadata("/minigames");
export default function Minigames(){return <ContentPage path="/minigames" source="minigames" title="Focused chess practice with a purpose." intro="Practice calculation, visualization, and pawn endings with Blundr’s three production standalone minigames." sections={[{heading:"Three standalone modes",body:<div className="grid">{games.map(game=><article className="card" key={game}><h3>{game}</h3><p>Focused practice with a clear chess-training purpose.</p></article>)}</div>},{heading:"Daily Blundr activities are separate",body:<p>Candidate Choice, Plan Recall, Same Position/Different Route, Continuation Challenge, Punish the Mistake, and Mixed Test belong to Daily Blundr. They are not additional standalone minigames.</p>}]}/>}
