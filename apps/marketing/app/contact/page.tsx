import type { Metadata } from "next"; import { ContentPage } from "../../components/ContentPage"; import { pageMetadata, SUPPORT_EMAIL } from "../../lib/site";
export const metadata: Metadata = pageMetadata("/contact", true);
export default function Contact(){return <ContentPage path="/contact" source="direct" title="Contact Blundr support" intro="Tell us what you need without sending secrets." sections={[{heading:"Choose a category",body:<ul><li>Account help</li><li>Provider import</li><li>Subscription or billing</li><li>Privacy or deletion</li><li>Technical support</li></ul>},{heading:"Use your mail app",body:<p>This marketing package has no approved support-form transport, so it does not pretend to submit a ticket. <a className="button" href={`mailto:${SUPPORT_EMAIL}?subject=Blundr%20support%20request`}>Email {SUPPORT_EMAIL}</a></p>},{heading:"Please do not include",body:<p>Never send passwords, provider tokens, or full PGNs unless support specifically requests a safe redacted excerpt.</p>}]}/>}
