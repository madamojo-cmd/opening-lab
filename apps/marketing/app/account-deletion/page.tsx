import type { Metadata } from "next"; import { LegalPage } from "../../components/LegalPage"; import { pageMetadata, SUPPORT_EMAIL } from "../../lib/site";
export const metadata: Metadata = pageMetadata("/account-deletion");
export default function AccountDeletion(){return <LegalPage path="/account-deletion" title="Account and data deletion" intro="Learn how to permanently delete your Blundr account, training history, imported-game data, and connected-provider information." sections={[{heading:"Delete in the app",body:<p>Sign in and use Settings → Account deletion. Reauthentication may be required before the destructive action.</p>},{heading:"Need help signing in?",body:<p>Email <a href={`mailto:${SUPPORT_EMAIL}`}>{SUPPORT_EMAIL}</a>. Do not send your password or provider token.</p>},{heading:"What deletion means",body:<p>Account deletion is intended to remove the account and associated product data according to the accepted app deletion contract and applicable retention requirements. Provider connections must not be treated as active after deletion.</p>}]}/>}
