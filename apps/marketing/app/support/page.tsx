import type { Metadata } from "next"; import { ContentPage } from "../../components/ContentPage"; import { pageMetadata, SUPPORT_EMAIL } from "../../lib/site";
export const metadata: Metadata = pageMetadata("/support");
export default function Support(){return <ContentPage path="/support" source="direct" title="How can we help?" intro="Accounts, billing, provider imports, privacy, and deletion support in one place." sections={[{heading:"Accounts and training",body:<p>For sign-in, onboarding, training, or progress questions, email <a href={`mailto:${SUPPORT_EMAIL}`}>{SUPPORT_EMAIL}</a>.</p>},{heading:"Providers and imports",body:<p>Ask about Chess.com or Lichess imports without sending passwords, provider tokens, or full PGNs by default.</p>},{heading:"Billing",body:<p>Include the plan and transaction context you can safely share. Apple App Store refunds are handled through Apple.</p>},{heading:"Privacy and deletion",body:<p>Read <a href="/privacy">Privacy</a> or <a href="/account-deletion">Account deletion</a>.</p>},{heading:"Response target",body:<p>We aim to respond to account and billing requests within two business days. <a href="/contact">Contact support</a>.</p>}]}/>}
