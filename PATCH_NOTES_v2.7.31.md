# Blundr v2.7.31 Patch Notes

- Removed automatic `/api/brain` calls from normal trainer position updates.
- Removed automatic `/api/brain` calls after wrong restricted moves.
- Added Assisted/Plain trainer view state.
- Added a minimal Pattern Cue Card to replace the visible Plan View panel.
- Plain View now hides pre-move visual hints while preserving normal board interaction.
- Manual Reveal still preserves `/api/brain`.
- `/api/blundr-visual-model` remains the default visual teaching source.
- This branch intentionally does not implement Maia, Stockfish top-two validation, or full bot refactor.
